
<h4>カフェ名</h4>
<%= @cafe.cafe_name %>

<% @cafe.tags.each do |tag| %>
    <%= tag.name %>
<% end %>

<h4>住所</h4>
<%= @cafe.address %>

<%= link_to "投稿する", new_cafe_post_path(cafe_id: @cafe.id) %>



<%= link_to "編集", edit_cafe_path(@cafe.id) %>
